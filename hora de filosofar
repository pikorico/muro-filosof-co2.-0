<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Muro Filosófico 🧠</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f5f5f5;
      padding: 20px;
      max-width: 600px;
      margin: auto;
    }
    h1 {
      text-align: center;
    }
    .card {
      background: white;
      padding: 15px;
      border-radius: 10px;
      margin: 10px 0;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }
    textarea {
      width: 100%;
      height: 100px;
      margin-top: 10px;
    }
    button {
      padding: 10px;
      background: #008CBA;
      color: white;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      margin-top: 10px;
    }
  </style>
</head>
<body>

  <h1>🧠 Muro Filosófico</h1>
  <div class="card" id="frase"></div>
  <div class="card" id="pregunta"></div>

  <div class="card">
    <p>Escribe tu reflexión:</p>
    <textarea id="reflexionInput"></textarea>
    <button onclick="enviarReflexion()">Enviar reflexión</button>
  </div>

  <div class="card">
    <h3>Reflexiones de otros estudiantes:</h3>
    <div id="reflexionesContainer"></div>
  </div>

  <script type="module">
    import { initializeApp } from "https://www.gstatic.com/firebasejs/10.12.2/firebase-app.js";
    import { getFirestore, collection, addDoc, getDocs } from "https://www.gstatic.com/firebasejs/10.12.2/firebase-firestore.js";

    const firebaseConfig = {
      apiKey: "AIzaSyDRIJXjmI9AOp7avFtes-e3SQ2W6d5Ph_g",
      authDomain: "muro-filosofico.firebaseapp.com",
      projectId: "muro-filosofico",
      storageBucket: "muro-filosofico.appspot.com",
      messagingSenderId: "67690213464",
      appId: "1:67690213464:web:9aa256cb22077ec4615a2d"
    };

    const app = initializeApp(firebaseConfig);
    const db = getFirestore(app);

    const frases = [
      "Solo sé que nada sé. – Sócrates",
      "El hombre está condenado a ser libre. – Sartre",
      "Pienso, luego existo. – Descartes",
      "La vida no examinada no vale la pena ser vivida. – Sócrates",
      "Dios ha muerto. – Nietzsche"
    ];

    const preguntas = [
      "¿Qué significa para ti la libertad?",
      "¿Es más importante la verdad o la felicidad?",
      "¿Qué te hace ser tú?",
      "¿Existe el destino o todo es casualidad?",
      "¿Qué valoras más: lo que tienes o lo que eres?"
    ];

    document.getElementById("frase").innerText = frases[Math.floor(Math.random() * frases.length)];
    document.getElementById("pregunta").innerText = "🗨️ " + preguntas[Math.floor(Math.random() * preguntas.length)];

    async function enviarReflexion() {
      const reflexion = document.getElementById("reflexionInput").value.trim();
      if (reflexion === "") return alert("Escribe una reflexión antes de enviar.");
      await addDoc(collection(db, "reflexiones"), {
        texto: reflexion,
        timestamp: Date.now()
      });
      document.getElementById("reflexionInput").value = "";
      obtenerReflexiones();
    }

    async function obtenerReflexiones() {
      const querySnapshot = await getDocs(collection(db, "reflexiones"));
      const container = document.getElementById("reflexionesContainer");
      container.innerHTML = "";
      querySnapshot.forEach((doc) => {
        const r = document.createElement("p");
        r.textContent = "📝 " + doc.data().texto;
        container.appendChild(r);
      });
    }

    obtenerReflexiones();
  </script>

</body>
</html>
